/** Shared chrome clearances so floating Account + bottom nav don't cover CTAs. */

/** Right-side header spacer / title padding under AppAccountButton. */
export const ACCOUNT_HEADER_CLEARANCE = 96;

/** Extra scroll/safe padding when AppBottomBar is on screen. */
export const BOTTOM_BAR_CLEARANCE = 120;

/** Min width for header back chips so labels like „Zurück“ don’t wrap. */
export const HEADER_BACK_CHIP_MIN_WIDTH = 76;
